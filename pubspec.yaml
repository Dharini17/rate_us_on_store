name: rate_us_on_store
description: "Open application detail page on PlayStore if android application and in AppStore if IOS application"
version: 0.0.2
homepage: https://github.com/Dharini17/rate_us_on_store
documentation: https://pub.dev/documentation/rate_us_on_store

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0